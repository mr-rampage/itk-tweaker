<StackPanel x:Class="Itk.Tweaker.Ui.Components.DicomImage"
            x:Name="Root"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:components="clr-namespace:Itk.Tweaker.Ui.Components"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            >
    <StackPanel.Resources>
        <materialDesign:NullableToVisibilityConverter x:Key="NullVisibilityConverter" />
        <components:ToggleVisibilityConverter x:Key="ToggleVisibilityConverter" />
    </StackPanel.Resources>
    <components:SelectImageButton
        Click="OnDicomImageSelected"
        Visibility="{Binding 
            ElementName=ImageThumbnail,
                Path=Visibility,
                Converter={StaticResource ToggleVisibilityConverter}}" />
    <Image
        Name="ImageThumbnail"
        Source="{Binding 
            RelativeSource={RelativeSource AncestorType=components:DicomImage},
            Path=Thumbnail,
            Mode=OneWay}"
        Visibility="{Binding
            RelativeSource={RelativeSource Self},
            Path=Source,
            Converter={StaticResource NullVisibilityConverter}}" />
</StackPanel>